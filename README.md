# FarmWise

A smart agriculture planning app scaffold.

## Quick start

1. Ensure Flutter SDK is installed: https://flutter.dev/docs/get-started/install
2. From this folder run:

```bash
flutter pub get
flutter run
```

Note: If you want platform folders (android/ios) generated, run `flutter create .` in this directory.
